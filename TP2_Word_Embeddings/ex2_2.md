## 1. What is Cosine Similarity?
https://fr.wikipedia.org/wiki/Similarit%C3%A9_cosinus

Cosine similarity is a measure of similarity between two non-zero vectors of an inner product space that measures the cosine of the angle between them. The cosine of the angle between two vectors gives an indication of how similar they are, regardless of their magnitude. It is often used in text mining and information retrieval to compare documents or terms.

The cosine similarity ranges from -1 to 1:

=> 1 means the vectors are identical in direction.
=> 0 means the vectors are orthogonal (no similarity).
=> -1 means the vectors are diametrically opposed.

## 2. In Python
see cos.py

## 3. Cosine Similarity Between Given Words
To compute the cosine similarity between words like "dog" and "cat", and "dog" and "dentist", you need to have their vector representations. Typically, these vectors come from word embeddings like Word2Vec, GloVe, or FastText.

### Exemple
dog = np.array([0.1, 0.2, 0.3])
cat = np.array([0.2, 0.3, 0.4])
dentist = np.array([0.8, 0.7, 0.1])

### Use previous fct
cos_sim_dog_cat = cosine_similarity_numpy(dog, cat)
